[project]
name = "publish-blog"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "keyring>=25.7.0",
    "markdown>=3.10.2",
    "markdownify>=1.2.2",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
]
